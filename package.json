{
  "name": "nrf24l01-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm install --prefix frontend --legacy-peer-deps && npm run build --prefix frontend"
  }
}
