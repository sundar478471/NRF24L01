[project]
name = "app"
version = "0.1.0"
dependencies = [ "fastapi>=0.100.0", "uvicorn>=0.22.0", "sqlalchemy>=2.0.0", "pydantic>=2.0.0", "pydantic-settings>=2.0.0", "web3>=6.5.0", "websockets>=11.0.0", "python-multipart>=0.0.6", "pytest>=7.3.1", "httpx>=0.24.1", "passlib[bcrypt]>=1.7.4", "cryptography>=41.0.0", "psycopg2-binary>=2.9.0" ]
requires-python = "~=3.13.0"
